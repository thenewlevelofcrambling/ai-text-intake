# auto: Anthropic when a key is present, otherwise mock
# anthropic: always use Anthropic and fail when the key is missing
# mock: always use the local deterministic analyzer
ANALYZER_MODE=auto
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
ANTHROPIC_TIMEOUT_SECONDS=20
